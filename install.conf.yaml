- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
#    ~/.vim: vim
#    ~/.vimrc: vimrc

- create:
    - ~/Work
    - ~/Dev/python_scripts

- shell:
#  - [git submodule update --init --recursive, Installing submodules]
    - sleep 5
    -
      command: ./test.zsh
      stdout: true
      stderr: true
